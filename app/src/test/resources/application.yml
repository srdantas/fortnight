operations:
  debit:
    bonus: 0.0
  withdraw:
    tax: 0.0

spring:
  profiles:
    active: tests
  datasource:
    url: 'jdbc:h2:mem:fortnight;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MYSQL;DATABASE_TO_UPPER=FALSE'
